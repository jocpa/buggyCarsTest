{
  "name": "buggycars",
  "version": "1.0.0",
  "description": "Pruebas al sitio de buggy cars",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-chrome": "cypress run --spec cypress/e2e/ --browser chrome",
    "test-allure": "cypress run --spec cypress/e2e/ --browser chrome --env allure=true"
  },
  "author": "jocpa",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "cypress": "^12.14.0",
    "cypress-mochawesome-reporter": "^3.5.1"
  }
}
